# sensehat-prometheus-explorer

This is a Prometheus exporter for the Raspberry Pi SenseHat which exports the
default metrics.

## Installation

Run

```sh
sudo apt-get install sensehat
pipenv install -D
```

## Running it

Run

```sh
python server.py
```
